<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_select"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <TextView
        android:text="TextView02"
        android:layout_height="wrap_content"
        android:id="@+id/itemTitle"
        android:layout_marginTop="16dp"
        app:layout_constraintTop_toTopOf="@+id/activity_select"
        app:layout_constraintLeft_toLeftOf="@+id/activity_select"
        app:layout_constraintBottom_toBottomOf="@+id/activity_select"
        android:layout_marginBottom="16dp"
        app:layout_constraintRight_toLeftOf="@+id/itemImage"
        android:layout_width="300dp">
    </TextView>

    <ImageView
        android:id="@+id/itemImage"
        android:layout_height="wrap_content"
        android:layout_width="wrap_content"
        app:layout_constraintTop_toTopOf="@+id/activity_select"
        app:layout_constraintRight_toRightOf="@+id/activity_select"
        android:layout_marginEnd="24dp"
        android:layout_marginRight="24dp"
        android:layout_marginTop="16dp">
    </ImageView>
</android.support.constraint.ConstraintLayout>